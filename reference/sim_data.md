# Simulated data

Toy example data provided by Alexandra Young and modified by adding
variables `Sex` and `Age`

## Usage

``` r
sim_data
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 25
rows and 10 columns.
